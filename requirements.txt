# === Core Framework ===
Flask==3.0.1
flask-cors==4.0.0
gunicorn==21.2.0

# === PDF Processing ===
PyPDF2==3.0.1            # Text extraction (used in text comparison)
PyMuPDF==1.24.9          # Render PDF pages as images (fitz)
pdf-diff==0.1.1          # Optional text bounding-box comparison
reportlab==4.1.0         # Optional PDF generation (if you return visual diff as PDF)

# === Image Processing ===
Pillow==10.2.0           # Create visual highlights, side-by-side display
opencv-python==4.10.0.84 # Optional, for more accurate visual pixel comparison

# === Utilities ===
requests==2.31.0         # For external API calls or webhooks
numpy==1.26.4            # Used internally by OpenCV
